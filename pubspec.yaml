name: effective_flutter_lab
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.8.0-91.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  scrollable_positioned_list: ^0.3.8
  dio: ^5.8.0+1
  flutter_bloc: ^9.1.0
  get_it: ^8.0.3
  drift: ^2.26.0
  sqlite3: ^2.7.5
  path_provider: ^2.1.5
  cached_network_image: ^3.4.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  drift_dev: ^2.26.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true

  assets:
    - assets/png/coffe_image.png
  #   - images/a_dot_ham.jpeg